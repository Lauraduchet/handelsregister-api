services:
  - type: web
    name: handelsregister-api
    env: python
    buildCommand: "pip install -r requirements.txt && pip install git+https://github.com/bundesAPI/handelsregister.git"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
